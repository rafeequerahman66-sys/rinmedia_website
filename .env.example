# Copy this file to .env and fill in your real keys.
# .env is git-ignored and never reaches the browser bundle.

# Brandfetch — secret Brand API key (server-side only)
# Get one at https://brandfetch.com/developers
BRANDFETCH_API_KEY=your_brandfetch_secret_key_here
